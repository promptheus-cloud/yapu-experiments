---
phase: 04.1-visual-fidelity
plan: 02
subsystem: ui
tags: [tailwind, pages, visual-fidelity, impact, hero, gradients, backgrounds]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: White navigation, Hero bgClass prop, rounded-full CTAs, bg-gray-50 shared components
  - phase: 04-page-assembly
    provides: All 7 page files with full content assembled
provides:
  - All 7 page files using bg-gray-50 for light alternating sections (no bg-muted/30 or bg-muted/5)
  - Impact page with gold gradient hero (linear-gradient D5BA00) — replaces standard Hero component
  - Impact page partner logos in full color (grayscale filter removed)
  - Investor Services Path to Resilience circles using bg-brand (dark teal), not bg-accent (mint)
  - About page contact submit button using bg-cta (orange) with rounded-full pill shape
  - NewsFilter pagination buttons pill-shaped (rounded-full)
  - Human visual verification at 1440px and 375px — approved
affects:
  - phase 05 (Compliance) — all page files are now the visual baseline for further additions
  - any page that adds new sections (should follow bg-gray-50 alternating pattern)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Inline hero section with linear-gradient style prop for page-unique hero backgrounds (Impact pattern)
    - bg-gray-50 for all alternating section backgrounds — replaces bg-muted/30 and bg-muted/5 throughout page files
    - bg-brand for numbered circle elements — dark teal, not mint, for sequence/step UI

key-files:
  created: []
  modified:
    - app/[locale]/page.tsx
    - app/[locale]/investor-services/page.tsx
    - app/[locale]/data-insights/page.tsx
    - app/[locale]/digital-tools/page.tsx
    - app/[locale]/impact/page.tsx
    - app/[locale]/news/page.tsx
    - app/[locale]/about/page.tsx
    - components/NewsFilter.tsx

key-decisions:
  - "Impact page hero replaced with inline section using linear-gradient(90deg, rgba(213,186,0,1) 0%, rgba(213,186,0,0.85) 100%) — Hero component import removed from this page"
  - "bg-gray-50 is the canonical alternating section background for all pages — bg-muted/30 and bg-muted/5 are not used"
  - "Partner logos on Impact page shown in full color — grayscale hover effect removed; original yapu.solutions shows color logos"
  - "Investor Services Path to Resilience circles use bg-brand (dark teal) not bg-accent (mint) — step indicators use brand color"
  - "About contact submit button uses bg-cta (orange) — aligns CTA color with global CTA language"

patterns-established:
  - "Inline hero section with style={{ background: 'linear-gradient(...)' }} for pages requiring unique hero backgrounds"
  - "bg-gray-50 alternating sections — apply consistently to all light-background sections in every page file"
  - "bg-brand for step/sequence number circles; bg-cta for call-to-action submit buttons"

requirements-completed: [VIS-03]

# Metrics
duration: ~2min (Task 1 code) + human verification
completed: 2026-02-27
---

# Phase 4.1 Plan 02: Page-Specific Visual Fixes Summary

**Page-level visual fidelity pass across all 7 pages: bg-gray-50 alternating sections, Impact gold gradient hero, full-color partner logos, dark teal step circles, and orange pill submit button — human-verified at 1440px and 375px**

## Performance

- **Duration:** ~2 min (code) + human verification
- **Started:** 2026-02-27T00:00:00Z
- **Completed:** 2026-02-27T00:19:15Z
- **Tasks:** 2 (1 code task + 1 human-verify checkpoint)
- **Files modified:** 8

## Accomplishments
- All 7 page files updated to use bg-gray-50 for light alternating sections — eliminates all bg-muted/30 and bg-muted/5 usage
- Impact page given a unique gold gradient hero (D5BA00) replacing the standard dark teal Hero component
- Impact partner logos shown in full color — grayscale filter removed to match yapu.solutions
- Investor Services Path to Resilience circles corrected to bg-brand (dark teal) — was incorrectly using mint
- About contact submit button changed to bg-cta (orange) + rounded-full — consistent CTA language
- NewsFilter pagination buttons converted to pill shape (rounded-full)
- Human visual verification passed — all pages confirmed visually equivalent to yapu.solutions at 1440px and 375px

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix section backgrounds and page-specific styles across all 7 pages + NewsFilter** - `074f751` (feat)
2. **Task 2: Visual verification checkpoint** - Human-approved (no code commit — verification only)

## Files Created/Modified
- `app/[locale]/page.tsx` - No changes needed (shared components already fixed in Plan 01)
- `app/[locale]/investor-services/page.tsx` - bg-gray-50 sections (3 occurrences), bg-brand circles, rounded-full CTA
- `app/[locale]/data-insights/page.tsx` - bg-gray-50 sections (3 occurrences)
- `app/[locale]/digital-tools/page.tsx` - bg-gray-50 in ContentSection helper + Features Grid section
- `app/[locale]/impact/page.tsx` - Inline gold gradient hero (replaces Hero component), bg-gray-50 section, grayscale filter removed
- `app/[locale]/news/page.tsx` - No changes needed
- `app/[locale]/about/page.tsx` - bg-gray-50 sections (2 occurrences), bg-cta + rounded-full submit button
- `components/NewsFilter.tsx` - rounded-full on Previous/Next pagination buttons

## Decisions Made
- Impact page hero implemented as inline section with `style={{ background: 'linear-gradient(...)' }}` rather than using the bgClass prop added in Plan 01 — the gold gradient is a style value not a class, and using inline style is simpler and more explicit for this one-off case
- Partner logos on Impact page shown in full color — the grayscale hover effect was a design assumption not present on the original yapu.solutions
- bg-gray-50 confirmed as the single canonical value for all alternating light sections across the entire site — bg-muted/30 and bg-muted/5 are no longer used in any page file

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Phase 4.1 Visual Fidelity is now complete — all 7 pages are pixel-accurate against yapu.solutions at 1440px and 375px
- Phase 5 (Compliance + Integration) can begin immediately — GDPR consent, analytics, newsletter, and SEO metadata
- All page files are a clean visual baseline; any new sections added in Phase 5 should follow the bg-gray-50 alternating pattern

---
*Phase: 04.1-visual-fidelity*
*Completed: 2026-02-27*

## Self-Check: PASSED

- Task 1 commit 074f751 confirmed in git log
- All 8 referenced files exist in the codebase
- Human visual verification approved by user
