---
phase: 04.2-visual-pixel-match
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Newsletter.tsx
  - components/PartnerCarousel.tsx
autonomous: false
requirements:
  - VIS-03

must_haves:
  truths:
    - "Newsletter section has dark teal background (bg-brand) with white text"
    - "Newsletter input fields have white background with brand-colored text on dark section"
    - "Newsletter submit button is mint (bg-accent) with pill shape, not orange"
    - "Homepage partner logos display in full color (no grayscale filter)"
    - "Visual comparison at 1440px confirms homepage matches yapu.solutions layout structure"
  artifacts:
    - path: "components/Newsletter.tsx"
      provides: "Dark teal newsletter section with white text and mint submit button"
      contains: "bg-brand"
    - path: "components/PartnerCarousel.tsx"
      provides: "Partner logos without grayscale filter"
  key_links:
    - from: "components/Newsletter.tsx"
      to: "messages/en.json"
      via: "useTranslations('Newsletter') for title, submitButton text"
      pattern: "useTranslations.*Newsletter"
---

<objective>
Restyle the Newsletter section to use dark teal background with white text and mint submit button matching yapu.solutions, remove grayscale filter from homepage partner logos, and verify all Phase 04.2 visual changes via human inspection.

Purpose: Newsletter background color and partner logo grayscale are the remaining high-impact visual gaps after the ServiceModules rebuild (Plan 01). This plan completes the homepage pixel-match effort.

Output: Restyled Newsletter component, updated PartnerCarousel, human-verified visual match.
</objective>

<execution_context>
@C:/Users/hmk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hmk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.2-visual-pixel-match/04.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/Newsletter.tsx (CURRENT):
```typescript
// "use client" component
// Uses: useTranslations('Newsletter'), useState, Input, Button
// Current bg: bg-accent/10
// Current button: bg-cta text-white (orange)
```

From components/PartnerCarousel.tsx (CURRENT):
```typescript
interface PartnerCarouselProps {
  title: string;
  logos: Logo[];
}
// Current: grayscale hover:grayscale-0 transition-all on Image
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Newsletter to dark teal background and remove PartnerCarousel grayscale</name>
  <files>
    components/Newsletter.tsx
    components/PartnerCarousel.tsx
  </files>
  <action>
**1. Update `components/Newsletter.tsx`:**

Change the outer section background from light mint to dark teal:
- `bg-accent/10` -> `bg-brand text-white`

Update all child text elements for contrast on dark background:
- Section title `<h2>`: change `text-foreground` to `text-white` (or remove it since parent has `text-white`)
- Description `<p>`: change `text-muted-foreground` to `text-white/80`
- Success message `<p>`: change `text-accent` to `text-accent` (keep — mint on dark teal is readable)

Update form inputs for contrast on dark background:
- All `<Input>` components: add className `bg-white text-brand placeholder:text-brand/60 border-0`
- This gives white input fields with dark teal text on the dark teal background

Update submit button:
- Change from `bg-cta text-white` to `bg-accent text-brand` (mint button with dark teal text, not orange)
- Keep `hover:opacity-90 w-full md:w-auto rounded-full` (pill shape from Phase 04.1)
- Add `font-semibold` if not already present

The final section element should be:
```tsx
<section className="bg-brand text-white py-16 px-6">
```

The title h2:
```tsx
<h2 className="text-3xl font-bold mb-4">{t('title')}</h2>
```

The description p:
```tsx
<p className="text-white/80 mb-8">{t('description')}</p>
```

Each Input:
```tsx
<Input
  ...existing props...
  className="bg-white text-brand placeholder:text-brand/60 border-0"
/>
```
(Remove any existing `className="mb-4"` from email input and move to wrapper or keep inline — ensure spacing is correct.)

The Button:
```tsx
<Button
  type="submit"
  className="bg-accent text-brand hover:opacity-90 w-full md:w-auto rounded-full font-semibold"
>
  {t('submitButton')}
</Button>
```

**2. Update `components/PartnerCarousel.tsx`:**

Remove grayscale filter from partner logos on the homepage. The original yapu.solutions shows partner logos in full color.

In the `<Image>` element, change:
- `className="object-contain h-10 w-auto grayscale hover:grayscale-0 transition-all"`
- To: `className="object-contain h-10 w-auto"`

This removes both the grayscale filter and the hover transition (logos always show in full color).

Note: Phase 04.1-02 already removed grayscale from Impact page partner logos. This change applies to the homepage PartnerCarousel component used on the main page.
  </action>
  <verify>
    <automated>cd C:/Users/hmk/promptheus/yapu2 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Newsletter section has dark teal (bg-brand) background with white text
- Newsletter inputs are white with dark teal placeholder text
- Newsletter submit button is mint (bg-accent) with pill shape
- Partner logos show in full color (no grayscale)
- TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — homepage pixel match at 1440px and 375px</name>
  <files>screenshots/</files>
  <action>
Phase 04.2 applies structural and content-level visual fixes to close the remaining gaps between the local rebuild and yapu.solutions:

1. ServiceModules — rebuilt from 4-column card grid to 4 stacked full-width sections with colored backgrounds, bullet lists, and "read more" links (Plan 01)
2. Hero content — headline changed to "YAPU facilitates resilience finance for the most vulnerable", CTA changed to "try the APP now" linking to https://my.yapu.solutions (Plan 01)
3. Newsletter — background changed from light mint to dark teal (bg-brand), submit button changed from orange to mint (bg-accent) (Plan 02)
4. Section titles — "Client voices", "Trusted by", "Our clients", "Stay up-to-date" (Plan 01)
5. Partner logos — grayscale filter removed, shown in full color (Plan 02)

Human verification steps:
1. Open http://localhost:3008/en/ at 1440px desktop width
2. Verify hero headline: "YAPU facilitates resilience finance for the most vulnerable"
3. Verify hero CTA button: "try the APP now" — click should go to https://my.yapu.solutions
4. Scroll down: verify 4 stacked full-width service sections (NOT a 4-column card grid):
   - Investor Services: darker gray background (rgb(217,217,217))
   - Data Insights: light gray background
   - Digital Tools: light gray background
   - Impact: dark teal background with white text
   - Each has uppercase title, bullet list, "read more" link
5. Verify "Client voices" section title (not "What Our Clients Say")
6. Verify "Trusted by" section title (not "Our Partners") — logos in full color
7. Verify "Our clients" section title (lowercase 'c')
8. Verify Newsletter section: dark teal background, white text, white input fields, mint submit button "Subscribe!"
9. Resize to 375px mobile — verify all sections stack correctly
10. Open http://localhost:3008/fr/ — verify French translations render
11. Open http://localhost:3008/es/ — verify Spanish translations render
12. Side-by-side comparison: Open yapu.solutions alongside localhost — compare homepage layout structure

Optional: Run node scripts/visual-compare.mjs home --viewport desktop --port 3008 to generate diff screenshot

Resume: Type "approved" to complete Phase 04.2, or describe any remaining visual issues
  </action>
  <verify>Human visual inspection confirms homepage matches yapu.solutions layout structure at 1440px and 375px</verify>
  <done>Homepage visual pixel match approved by human — Phase 04.2 complete</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. Newsletter section visually matches yapu.solutions dark teal treatment
3. Partner logos display in full color on homepage
4. Human verifies side-by-side visual match at 1440px desktop
5. All 3 locales render without errors
</verification>

<success_criteria>
- Newsletter has dark teal background with white text and mint submit button
- Partner logos show in full color (no grayscale filter)
- Human approves visual match between homepage and yapu.solutions at 1440px and 375px
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-visual-pixel-match/04.2-02-SUMMARY.md`
</output>
