---
phase: 04.2-visual-pixel-match
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ServiceModules.tsx
  - content/data/en/homepage.json
  - content/data/fr/homepage.json
  - content/data/es/homepage.json
  - app/[locale]/page.tsx
  - messages/en.json
  - messages/fr.json
  - messages/es.json
autonomous: true
requirements:
  - VIS-03

must_haves:
  truths:
    - "Homepage shows 4 stacked full-width service sections (not a 4-column card grid) with colored backgrounds and bullet lists"
    - "Investor Services section has bg-[rgb(217,217,217)], Data Insights and Digital Tools have bg-gray-50, Impact has bg-brand with white text"
    - "Each service section displays a title, bullet list of features, and a 'read more' link to the subpage"
    - "No 'Our Services' heading appears above the four service sections"
    - "Hero headline reads 'YAPU facilitates resilience finance for the most vulnerable' (EN)"
    - "Hero CTA reads 'try the APP now' and links to https://my.yapu.solutions"
    - "Testimonial section title reads 'Client voices', Partners title reads 'Trusted by'"
  artifacts:
    - path: "components/ServiceModules.tsx"
      provides: "Stacked full-width service sections with bullet lists"
      contains: "sectionConfig"
    - path: "content/data/en/homepage.json"
      provides: "Updated hero text and serviceModules with bullets arrays"
      contains: "bullets"
    - path: "app/[locale]/page.tsx"
      provides: "Updated ServiceModule interface and ServiceModules call without title prop"
      contains: "bullets"
  key_links:
    - from: "content/data/en/homepage.json"
      to: "app/[locale]/page.tsx"
      via: "readContent<HomepageContent> with updated ServiceModule interface"
      pattern: "bullets.*string\\[\\]"
    - from: "app/[locale]/page.tsx"
      to: "components/ServiceModules.tsx"
      via: "ServiceModules component receives modules prop (no title prop)"
      pattern: "ServiceModules.*modules"
---

<objective>
Rebuild ServiceModules from a 4-column card grid into 4 stacked full-width sections matching yapu.solutions, update homepage hero text and CTA to match the original, and fix all i18n UI string titles (testimonials, partners, clients, newsletter) across EN/FR/ES.

Purpose: The ServiceModules layout is the single largest visual mismatch between the rebuild and yapu.solutions. Combined with hero text and section title fixes, this plan closes ~70% of the remaining homepage visual gaps.

Output: Restructured ServiceModules component, updated homepage JSON (3 locales) with bullets schema, corrected messages (3 locales), updated page.tsx interface.
</objective>

<execution_context>
@C:/Users/hmk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hmk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.2-visual-pixel-match/04.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/ServiceModules.tsx (CURRENT — will be replaced):
```typescript
interface ServiceModule {
  id: string;
  title: string;
  description: string;
  icon: string;
  href: string;
}

interface ServiceModulesProps {
  title: string;
  modules: ServiceModule[];
}

export function ServiceModules({ title, modules }: ServiceModulesProps)
```

From app/[locale]/page.tsx (CURRENT — must update):
```typescript
interface ServiceModule {
  id: string;
  title: string;
  description: string;
  icon: string;
  href: string;
}

interface HomepageContent {
  hero: { title: string; subtitle: string; ctaText: string; ctaHref: string; };
  serviceModules: ServiceModule[];
  testimonial: { quote: string; author: string; role: string; organization: string; };
  partnerLogos: Logo[];
  clientLogos: Logo[];
}
```

From i18n/navigation.ts:
```typescript
import { Link } from '@/i18n/navigation';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild ServiceModules component, update JSON schema (3 locales), update page.tsx interface</name>
  <files>
    components/ServiceModules.tsx
    content/data/en/homepage.json
    content/data/fr/homepage.json
    content/data/es/homepage.json
    app/[locale]/page.tsx
  </files>
  <action>
**1. Replace `components/ServiceModules.tsx` entirely:**

Remove the current card grid implementation. Replace with stacked full-width sections. The new component:

- Removes the `title` prop (no "Our Services" heading above sections)
- Accepts only `modules` prop
- Uses a `sectionConfig` record keyed by module id for per-section styling
- Each section is a full-width `<section>` element with configurable background
- Content: `<h2>` title (uppercase tracking-wide), `<ul>` bullet list, "read more" `<Link>`

Section backgrounds (from RESEARCH.md WebFetch analysis):
- `investor-services`: `bg-[rgb(217,217,217)]` (darker gray), text-brand title, text-cta link
- `data-insights`: `bg-gray-50`, text-brand title, text-cta link
- `digital-tools`: `bg-gray-50`, text-brand title, text-cta link
- `impact`: `bg-brand` (dark teal), text-white title and text, text-accent link

Bullet rendering: flex row with a small circle span (`w-1.5 h-1.5 rounded-full bg-current flex-shrink-0 opacity-70`) as the bullet marker, then text.

Section layout: `py-16 px-6`, inner `max-w-4xl mx-auto`, title is `text-2xl font-bold mb-6 uppercase tracking-wide`, bullets have `space-y-3 mb-8`, link is `text-sm font-semibold uppercase tracking-wider hover:underline`.

Import `Link` from `@/i18n/navigation` (NOT next/link). No lucide-react imports, no shadcn Card imports.

New interface:
```typescript
interface ServiceModule {
  id: string;
  title: string;
  bullets: string[];
  readMoreText: string;
  href: string;
}
interface ServiceModulesProps {
  modules: ServiceModule[];
}
```

**2. Update all 3 locale homepage.json files:**

The `serviceModules` array must change schema: remove `description` and `icon` fields, add `bullets` (string array) and `readMoreText` (string).

EN (`content/data/en/homepage.json`):
```json
"hero": {
  "title": "YAPU facilitates resilience finance for the most vulnerable",
  "subtitle": "YAPU Solutions provides innovative digital tools that enable financial institutions, investors and development organizations to achieve sustainable and inclusive growth.",
  "ctaText": "try the APP now",
  "ctaHref": "https://my.yapu.solutions"
},
"serviceModules": [
  {
    "id": "investor-services",
    "title": "Investor Services",
    "bullets": ["Path to resilience", "Measure your impact", "Monitor your portfolio in real time"],
    "readMoreText": "read more",
    "href": "/en/investor-services"
  },
  {
    "id": "data-insights",
    "title": "Data Insights",
    "bullets": ["Loan decision support", "Climate, environmental and social risks", "Biodiversity risk", "Staff performance"],
    "readMoreText": "read more",
    "href": "/en/data-insights"
  },
  {
    "id": "digital-tools",
    "title": "Digital Tools",
    "bullets": ["Easy digital transformation", "Improved risk management", "Data insights"],
    "readMoreText": "read more",
    "href": "/en/digital-tools"
  },
  {
    "id": "impact",
    "title": "Impact",
    "bullets": ["Putting financial inclusion first", "Promoting green finance", "Fostering climate resilience for the most vulnerable"],
    "readMoreText": "read more",
    "href": "/en/impact"
  }
]
```

FR (`content/data/fr/homepage.json`):
- hero.title: "YAPU facilite le financement de la resilience pour les plus vulnerables"
- hero.ctaText: "essayez l'APP maintenant"
- hero.ctaHref: "https://my.yapu.solutions"
- serviceModules: same schema with FR translations for titles (keep existing FR titles), translated bullets, readMoreText: "en savoir plus", hrefs use /fr/ prefix

ES (`content/data/es/homepage.json`):
- hero.title: "YAPU facilita las finanzas de resiliencia para los mas vulnerables"
- hero.ctaText: "prueba la APP ahora"
- hero.ctaHref: "https://my.yapu.solutions"
- serviceModules: same schema with ES translations for titles (keep existing ES titles), translated bullets, readMoreText: "leer mas", hrefs use /es/ prefix

Keep `testimonial`, `partnerLogos`, `clientLogos` sections unchanged in all 3 files.

**3. Update `app/[locale]/page.tsx`:**

- Change the `ServiceModule` interface: remove `description` and `icon`, add `bullets: string[]` and `readMoreText: string`
- Remove the `servicesTitle` translation call (`t('servicesTitle')`)
- Change the `ServiceModules` JSX: remove `title={t('servicesTitle')}` prop — just pass `modules={content.serviceModules}`
- Keep all other sections unchanged
  </action>
  <verify>
    <automated>cd C:/Users/hmk/promptheus/yapu2 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- ServiceModules renders 4 stacked full-width sections with colored backgrounds and bullet lists (no card grid, no "Our Services" heading)
- Homepage JSON files in all 3 locales have updated hero text and serviceModules with bullets schema
- page.tsx ServiceModule interface matches new JSON schema
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update i18n message strings across all 3 locales</name>
  <files>
    messages/en.json
    messages/fr.json
    messages/es.json
  </files>
  <action>
Update the following keys in all 3 locale message files to match the original yapu.solutions section titles:

**EN (`messages/en.json`):**
- `Homepage.testimonialsTitle`: "What Our Clients Say" -> "Client voices"
- `Homepage.partnersTitle`: "Our Partners" -> "Trusted by"
- `Homepage.clientsTitle`: "Our Clients" -> "Our clients" (lowercase 'c')
- `Homepage.servicesTitle`: REMOVE this key entirely (no longer used — ServiceModules has no section title)
- `Newsletter.title`: "Stay Updated" -> "Stay up-to-date"
- `Newsletter.submitButton`: "Subscribe" -> "Subscribe!"

**FR (`messages/fr.json`):**
- `Homepage.testimonialsTitle`: "Ce que Disent Nos Clients" -> "Voix des clients"
- `Homepage.partnersTitle`: "Nos Partenaires" -> "Nos partenaires de confiance"
- `Homepage.clientsTitle`: "Nos Clients" -> "Nos clients" (lowercase 'c')
- `Homepage.servicesTitle`: REMOVE this key entirely
- `Newsletter.title`: "Restez Informe" -> "Restez a jour"
- `Newsletter.submitButton`: "S'abonner" -> "S'abonner!"

**ES (`messages/es.json`):**
- `Homepage.testimonialsTitle`: "Lo que Dicen Nuestros Clientes" -> "Voces de clientes"
- `Homepage.partnersTitle`: "Nuestros Socios" -> "Socios de confianza"
- `Homepage.clientsTitle`: "Nuestros Clientes" -> "Nuestros clientes" (lowercase 'c')
- `Homepage.servicesTitle`: REMOVE this key entirely
- `Newsletter.title`: "Mantengase Informado" -> "Mantengase al dia"
- `Newsletter.submitButton`: "Suscribirse" -> "Suscribirse!"

Do NOT change any other keys in the message files.
  </action>
  <verify>
    <automated>cd C:/Users/hmk/promptheus/yapu2 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- "Client voices" appears as testimonial section title (EN)
- "Trusted by" appears as partners section title (EN)
- "Stay up-to-date" appears as newsletter section title (EN)
- "Subscribe!" appears as newsletter button text (EN)
- Equivalent translations applied in FR and ES
- No "servicesTitle" key remains in any message file
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. Homepage at `/en/` shows 4 stacked full-width service sections (not a card grid) with correct backgrounds
3. No "Our Services" heading visible above the service sections
4. Hero headline reads "YAPU facilitates resilience finance for the most vulnerable"
5. Hero CTA reads "try the APP now" and links to https://my.yapu.solutions
6. "Client voices" section title visible instead of "What Our Clients Say"
7. "Trusted by" section title visible instead of "Our Partners"
8. "Stay up-to-date" newsletter title visible instead of "Stay Updated"
9. Impact service section has dark teal background with white text
10. `/fr/` and `/es/` pages render without errors and show translated content
</verification>

<success_criteria>
- Homepage ServiceModules shows 4 stacked full-width colored sections with bullet lists — not a 4-column card grid
- Hero text matches yapu.solutions original
- All section titles match yapu.solutions original
- All 3 locales compile and render without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-visual-pixel-match/04.2-01-SUMMARY.md`
</output>
